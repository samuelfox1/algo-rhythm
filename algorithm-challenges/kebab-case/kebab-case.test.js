const { describe, it, expect, toBe, beforeAll, afterAll } = require('@jest/globals')
const kebabCase = require('./kebab-case')

describe('kebab-case', () => {
    it('should take in a string and convert it to kebab-case', () => {

    })
})
